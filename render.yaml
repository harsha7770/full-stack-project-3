services: [{type: web, name: api, buildCommand: "npm install", startCommand: "npm start", rootDir: server}]
static_sites: [{name: app, buildCommand: "npm install && npm run build", publishDir: dist, rootDir: client}]
