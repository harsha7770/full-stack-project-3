services:
  - type: static
    name: mentorship-platform-frontend
    rootDir: client
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: http://localhost:8080 # Placeholder until backend is deployed
